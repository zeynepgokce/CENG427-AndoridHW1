<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zeynep.phonecontectrecovery">

    <uses-permission android:name="android.permission.READ_CONTACTS"/> // the permission to reach the person to read the contacts
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/> // the permission to reach the person to write the contacts

    <uses-permission android:name="android.permission.CALL_PHONE" />//the permission to call the clicked number

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
